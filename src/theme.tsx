import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    light_blue: '#2596be',
    dark_blue: '#295dc2',
    red: '#ca0000',
    white: '#f2f2f2'
  }
});

export default theme;